# Non-secret configuration for the GitHub Actions reference verifier.
# Values are parsed as literal key=value fields, not sourced as shell.
repository=Ernanidacosta/coding-agent-control
workflow=ci.yml
workflow_path=.github/workflows/ci.yml
